<Window x:Class="InitialProject.WPF.Views.Guest2Views.VouchersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InitialProject.WPF.Views.Guest2Views"
        mc:Ignorable="d"
        Title="VouchersView" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="DataGridTours" ItemsSource="{Binding Vouchers, UpdateSourceTrigger=PropertyChanged}" SelectionMode="Single" AutoGenerateColumns="False" IsReadOnly="True" Margin="114,46,114,64">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Width="280" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Expiry Date" Width="300" Binding="{Binding ExpiryDate}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
