<Window x:Class="InitialProject.WPF.Views.OwnerViews.RatedGuestsOverview"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InitialProject.WPF.Views.OwnerViews"
        mc:Ignorable="d"
        Title="RatedGuestsOveview" Height="350" Width="700">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <DataGrid Name="DataGridGuests" IsReadOnly="True" SelectionMode="Single" AutoGenerateColumns="False" Height="325" HeadersVisibility="Column" FontSize="14">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Guest name" Width="120"/>
                    <DataGridTextColumn Header="Accommodation name" Width="200"/>
                    <DataGridTextColumn Header="Contry" Width="110"/>
                    <DataGridTextColumn Header="City" Width="110"/>
                    <DataGridTemplateColumn Header="See rating" Width="100*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Name="ButtonSeeRating" Content="See rating"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="ButtonCancel" Content="Close" Width="100" Height="40" Margin="30,0,30,0"/>
        </StackPanel>

    </Grid>
</Window>
